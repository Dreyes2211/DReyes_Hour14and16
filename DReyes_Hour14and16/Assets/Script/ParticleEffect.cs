using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ParticleEffect : MonoBehaviour
{
// changed the shape of the emittor so the flow would change//

// changed the velocity over lifetime to make more lighting like//

//changed size by speed so it tappers of at the end//

//added ribbon trail to give it lighting effect//

//changed color to make it look like a lighting bolt// 
}
